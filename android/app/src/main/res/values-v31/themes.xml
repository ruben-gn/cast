<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Cast" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowBackground">@color/linen_background</item>
        <!-- System splash: launcher icon centered on the same navy as the icon background,
             so the splash reads as one designed mark. -->
        <item name="android:windowSplashScreenBackground">@color/ic_launcher_background</item>
    </style>
</resources>
