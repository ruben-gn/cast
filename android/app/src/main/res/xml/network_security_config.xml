<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP to local servers (cast.local, LAN IPs, etc.) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
