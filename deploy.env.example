# Copy this file to `deploy.env` (gitignored) and fill in your values.
# Sourced at runtime by deploy.sh and deploy-mac.sh.

# deploy-mac.sh: SSH/SCP target for the backend deploy
PI_HOST=your-host-or-ip
PI_USER=your-user

# deploy.sh: host shown in the printed "Webapp:" URL
HOST=your-host-or-ip
